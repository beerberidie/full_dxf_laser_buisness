# WhatsApp Connector Suite

- backend/ — FastAPI connector
- frontend/ — React admin UI (Vite + Tailwind via CDN)

See each subfolder for quick start. Configure `.env` in backend, then open frontend and set Base URL to your backend.
